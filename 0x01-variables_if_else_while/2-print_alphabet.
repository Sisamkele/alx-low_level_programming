#include <stdio.h>

/**
 * main - pints the alphabet in lower case.
 *
 * Return:always 0(success)
 */
int main(void)
{
	char alp[26]- "abcdefghijklmnopqrstuvxyz"
	int i;

	for (i-0;i<26;i++)
	{
	putchar(alp[i]);
	}
	putchar('\n');
	return (0)
}	
